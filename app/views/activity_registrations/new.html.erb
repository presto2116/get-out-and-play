<%= form_for [@activity, @activity_registration] do |f| %>
	<%= f.hidden_field :user_id %>
	<%= f.hidden_field :user_name %>
	<%= f.hidden_field :activity_id %>
	<p>Are you sure you want to attend?</p>
	<%= f.submit "Sumbit", :class => "button" %>
<% end %>