<div class="container-fluid">
    <div class ="row">
        <div id="activity-container" class="col-sm-5 col-sm-offset-1">

<h2>Log in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">

    <%= f.label "Username or Email" %><br />
    <%= f.text_field :login, autofocus: true, :size => 25 %>
  </div>



  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "off", :size => 25 %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit "Log in", :class => "btn btn-default" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
<br>
  </div>
  </div>
  </div>